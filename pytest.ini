[pytest]
# importlib mode: import test modules without requiring __init__.py packages or
# sys.path munging. The plugin package itself is loaded by tests/conftest.py,
# which mirrors how Hermes loads a plugin (by directory name, via importlib).
addopts = --import-mode=importlib -q
testpaths = tests
